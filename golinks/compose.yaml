services:
  golink:
    image: ghcr.io/tailscale/golink:main
    container_name: golink
    restart: unless-stopped
    user: root
    environment:
      - TS_AUTHKEY=tskey-auth-REPLACE_ME
    volumes:
      - /volume1/docker/golink/data:/home/nonroot
